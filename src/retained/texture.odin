package retained;

import types "../types";

create_texture :: proc() {
    panic("Unimplemented");
}

destroy_texture :: proc() {
    panic("Unimplemented");
}

get_last_texture :: proc() -> types.Result(u32) {
    panic("Implemented");
}