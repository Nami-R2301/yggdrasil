package ygg;

import "types";

////////////////////////////////////////////////////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////////////////////////////////
/////////////////////////////////////////////// LOW LEVEL API //////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////////////////////////////////

//TODO: Create renderer with default features.
_create_renderer :: proc (type: types.RendererType = types.RendererType.OpenGL, bg_color: u32 = 0x181818) -> (types.RendererError, types.Option(types.Renderer))  {
    panic("Unimplemented");
}

// TODO: Deallocate and free up buffers in the pipeline for graceful shutdown.
_destroy_renderer :: proc (renderer: ^types.Renderer) -> types.RendererError {
    panic("Unimplemented");
}